# Agent Harness 设计要点

Agent 不是模型直接执行动作，而是 Model + Harness。

Harness 负责：

- 构造上下文和工具描述。
- 校验模型输出的工具参数。
- 按权限决定是否执行工具。
- 把工具结果、错误、超时和取消写回状态。
- 保存 Trace，支持问题复盘和回归测试。

生产系统需要设置最大步数、最大 token 预算、工具超时、重复调用检测和人工接管边界。
Prompt 只能表达偏好，安全边界必须由代码实现。
